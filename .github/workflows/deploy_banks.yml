name: deploy banks image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: Immutable Docker image tag to deploy
        required: true
        type: string
    
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:

      - name: Deploy the selected banks image
        id: deploy_banks
        uses: appleboy/ssh-action@master
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 10000
          envs: IMAGE_TAG
          script: |
            set -Eeuo pipefail
            /srv/cluster/current/tools/set-image-tag.sh banks "$IMAGE_TAG"
            /srv/cluster/current/tools/start-stack.sh application
